## 一句话总结

这个工具是一个“代码风险扫描器”，专门检查 C/C++ 代码里常见的线程和内存安全问题，并给出修复建议。

## 具体做了什么

1. 先把代码里的注释去掉，避免误判。
2. 扫描危险函数，比如 strcpy。
3. 扫描并发风险，比如：
   - 进入临界区后提前 return，锁没释放，可能死锁；
   - 使用 TerminateThread 这种高风险 API。
4. 输出 HTML 报告：风险类型、位置、代码片段、修复建议，必要时还会给“执行路径”。

## 如何使用

一键运行 python 文件即可得到报告

```bash
python threadviper.py 测试用例1.txt --html report.html
```

使用 py 内置测试框架 unittest 自动测试文件

```bash
python -m unittest -v
```

## 可能的问题

准确吗？
“它针对明确规则（危险 API、临界区退出等）检测，准确性高；同时有自动测试持续校验。”

能替代人工审查吗？
“不能完全替代，但能做第一道自动筛查，减少人工负担和漏检。”

业务价值是什么？
“提前拦截死锁/内存风险，减少线上事故和排查成本。”